#include <sys/mman.h>
#include <sys/stat.h>        /* mode 定数用 */
#include <fcntl.h>           /* O_* 定数の定義用 */

shmem_open()
{
	void* ptr;
	char* filename = "/shm_mempolicy";

	(void)shm_unlink(filename);

	ptr = shm_open(filename, O_RDWR|O_CREAT|O_TRUNC, 0622);

	(void)shm_unlink(filename);
}
